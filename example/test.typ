#import "../typst/lib.typ": emoji-list

#set text(font: "MS Mincho")

```typ
#emoji-list(
  "\example\emoji_list.csv",
  [おはようございます。],
  [こんにちは！],
  [こんばんは。]
)
```

を実行すると

#emoji-list(
  "\example\emoji_list.csv",
  [おはようございます。],
  [こんにちは！],
  [こんばんは。]
)


```typ
#emoji-list(
  "\example\emoji_list.csv",
  [雪、無音、窓辺にて],
  [ウェカピポ],
  [無重力シャトル]
)

```

#emoji-list(
  "\example\emoji_list.csv",
  [雪、無音、窓辺にて],
  [ウェカピポ],
  [無重力シャトル]
)
